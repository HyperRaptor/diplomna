using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Button_2 : MonoBehaviour
{
    
    /*public int normalSpeed, fastSpeed;
    bool pressed = false;
    void OnMouseDown(){
        pressed = !pressed;
    }

    void Update()
    {
        if(Unit_manager.Instance.Guns.Count > 0){
            if(pressed){
                Debug.Log("Normie");
                foreach(GameObject gun in Unit_manager.Instance.Guns){
                    gun.GetComponent<spiral_movement>().speed = fastSpeed;
                }
            }
            else{
                //Debug.Log("Fast");
                foreach(GameObject gun in Unit_manager.Instance.Guns){
                    gun.GetComponent<spiral_movement>().speed = normalSpeed;
                }
            }
        }
        //pressed = false;
    }*/
}
